# kaneai-nodejs-demo

# My Node Sample App

This is a minimal Express-based Node.js server created for demonstration purposes.  
It provides a few simple endpoints to showcase LambdaTest AI-powered test generation.

---

## Features

- **GET /** → returns a welcome message  
- **GET /api/echo?msg=<text>** → returns a JSON response `{ "echo": "<text>" }`  

---

## Installation

1. Clone the repository:

```bash
git clone https://github.com/sriramlt/kaneai-nodejs-demo.git
cd kaneai-nodejs-demo
